<form method="post" action="{{ path('stage_delete', {'id': stage.id}) }}" onsubmit="return confirm('Voulez vous vraiment supprimer ce stage ? Cette action est irréversible');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ stage.id) }}">
    <button class="btn btn-primary">Supprimer</button>
</form>
